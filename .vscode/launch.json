{
    "version": "0.2.0",
    "configurations": [
        {
            "name": "Get Slack History",
            "type": "go",
            "request": "launch",
            "mode": "auto",
            "program": "slack/main.go",
            "envFile": "${workspaceFolder}/.env"
        }
    ]
}